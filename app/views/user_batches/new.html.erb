<%= flash_and_form_errors(@user_batch) %>
<%= nice_form_for(@user_batch) do |f| %>
  <%= form_field(f, :users, :type => :textarea, :required => true) %>
  <%= form_submit_button(f, :label => :create) %>
<% end %>