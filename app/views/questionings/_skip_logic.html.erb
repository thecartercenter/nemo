<% if read_only || cannot?(:update_core, @questioning) %>
<% else %>
  <div id="skip-logic-form-fields">
    <%= react_component("SkipLogicFormField", QuestioningSerializer.new(@questioning)) %>
  </div>
<% end %>
