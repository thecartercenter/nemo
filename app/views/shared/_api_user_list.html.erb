<h3>Select users</h3>

<div id="api_users_scrollbox">
  <hr>
  <% @users.each do |user| %>
    <li>
      <%= check_box_tag "whitelist_users[]", user.id, @form.whitelist_users.pluck(:user_id).include?(user.id)  %>
      <%= user.name %>
    </li>
  <% end %>
</div>
