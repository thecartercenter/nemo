<%= form.select(:repeat_count_qing_id,
    [["[#{t("common.none")}]", nil]] +
      @qing_group.form.questionings
        .select { |qing| %w[integer counter].include?(qing.question.qtype_name) }
        .map { |qing| [qing.code, qing.id] }) %>
