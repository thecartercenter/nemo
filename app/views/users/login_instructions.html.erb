<%# no title because the title is incorporated into the login instructions box %>
<% @dont_print_title = true %>

<%= flash_and_form_errors(@user) %>

<div id="login_instructions">
  <div id="preamble">
    <%= t("user.login_instructions.preamble_singular") %>
    <p><%= link_to(t("user.back_to_index"), users_path) %></p>
  </div>

  <%= render("login_instruction_box", :user => @user, :password => flash[:password]) %>
</div>