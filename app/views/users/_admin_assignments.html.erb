<%= content_tag(:div, assignment_errors.join(" "), class: "form-errors") unless assignment_errors.empty? %>

<div class="assignment-header">
  <div class="mission"><%= translate_model(Mission) %></div>
  <div class="role"><%= t('role.role') %></div>
</div>

<% if read_only %>
  <% @user.assignments.each do |a| %>
    <div class="assignment-row">
      <div class="mission"><%= a.mission.name %></div>
      <div class="role"><%= t("role.#{a.role}") %></div>
    </div>
  <% end %>
<% else %>
  <%= content_for(:per_page_packs, "user_assignment_form") %>
  <%= react_on_rails_component("UserAssignmentForm", props: @assignment_data) %>
<% end %>
