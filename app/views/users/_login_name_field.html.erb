<%= form.text_field(:login) %>
<% unless form.mode == :show %>
  <%= button_to_function(t("common.suggest"), "suggest_login(this)", :id => "login_suggest_button", :class => "btn_secondary") %>
<% end %>