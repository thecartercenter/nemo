<% if read_only %>

  <%= form.object.login %>

<% else %>

  <%= form.text_field(:login, :class => 'form-control') %>&nbsp;
  <%= form.button(t("common.suggest"), :class => "btn btn-default btn-xs")  %>

<% end %>