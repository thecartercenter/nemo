<% @title_args = {:code => @question.code} %>

<%= flash_and_form_errors(@question) %>

<%= nice_form_for(@question) do |f| %>
  <%= render('fields', :form => f) %>
  <%= form_submit_button(f, :label => :save) %> 
<% end %>