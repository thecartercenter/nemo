<% if read_only || cannot?(:update_tags, @question) %>

  <%= render_tags(form.object.tags.order(:name)) %>

<% else %>

  <%= text_field(:question, :tag_ids, {class: 'text form-control'}) %>

<% end %>
