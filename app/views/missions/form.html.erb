<% @title_args = {:name => @mission.name} %>

<%= flash_and_form_errors(@mission) %>
<%= nice_form_for(@mission) do |f| %>
  <%= form_field(f, :name, :required => true, :maxlength => 32) %>
  <%= form_submit_button(f, :label => :save) %>
<% end %>