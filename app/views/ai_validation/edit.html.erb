<% content_for :title, t("ai_validation.edit_rule") %>

<div class="ai-validation-form-page">
  <div class="page-header">
    <h1><%= t("ai_validation.edit_rule") %></h1>
    <%= link_to t("common.back"), ai_validation_rule_path(@rule), class: "btn btn-secondary" %>
  </div>

  <%= render "form" %>
</div>
