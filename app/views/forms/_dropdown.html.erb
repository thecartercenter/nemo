<% if @forms.any? %>
  <% @forms.each do |f| %>
    <%= content_tag("li", link_to(f.name, new_response_path(form_id: f.id))) %>
  <% end %>
<% else %>
  <%= content_tag("li", t("layout.no_live_forms")) %>
<% end %>
