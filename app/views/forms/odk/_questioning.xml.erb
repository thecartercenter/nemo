<% label_row = !!local_assigns[:label_row]; grid_mode = !!local_assigns[:grid_mode]; # Default values %>
<% group ||= nil %>
<%# Note that subqings here refers to multiple levels of a cascading select question, not question grouping. %>

<%# If group is a multilevel_fragment, we are supposed to just render one of the subqings here. %>
<%# This is so they can be wrapped with the appropriate group headers/hint and such. %>

<% subqings = group.try(:multilevel_fragment?) ? [qing.subqings[group.level - 1]] : qing.subqings %>
<% subqings.each do |subq| %>
  <%= subq.input_tag(grid_mode, label_row, xpath_prefix) %>
<% end %>
