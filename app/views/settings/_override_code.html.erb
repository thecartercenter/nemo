<%= form.object.override_code %> &nbsp;&nbsp;&nbsp;&nbsp;
<% unless form_mode == :show %>
  <input class="submit btn btn-default btn-sm" name="regenerate"
    data-confirm="<%= t('setting.override_code_warning') %>" type="submit" value="<%= t('common.regenerate') %>">
<% end %>
