<% answer_set.answers.each_with_index do |answer, i| %>
  <%= answer.level_name %>
  <%= select_tag("answer_sets[#{index}][answers][#{i}][option_id]", options_from_collection_for_select(answer_set.options_for(answer), 'id', 'name'),
    include_blank: true, class: 'form-control') %>
<% end %>