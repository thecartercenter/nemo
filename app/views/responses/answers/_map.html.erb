<div id="map_<%= answer.id %>" class="answer-map"></div>

<%= javascript_doc_ready do %>
  new ELMO.Views.AnswerMapView(<%=json({
    el: "#map_#{answer.id}",
    points: answer.latlngs,
    markerUrl: image_url('markers/small.png')
  })%>);
<% end %>
