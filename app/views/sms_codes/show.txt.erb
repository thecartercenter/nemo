<!-- TOM why all the raw calls here? raw is for escaping html. in any case, .html_safe is prefered over raw, 
  but i don't see why you need either here -->
<%= raw t 'sms.instructions.title' %>

<%= raw t 'sms.instructions.form_id', :form_id => @form_id %>
<%= raw t 'sms.instructions.question' %>

<%= raw t 'sms.instructions.spacing' %>

<%= raw t 'sms.instructions.response.select_one' %>

<%= raw t 'sms.instructions.response.select_multiple' %>

<%= raw t 'sms.instructions.response.number' %>


<% @codeText.each { |x| %>
  <%= raw x %>
<% } %>

