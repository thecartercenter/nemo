<table class="report_meta">
  <tr>
    <td>
      <%= I18n.t('report/report.mission') %>
    </td>
    <td>
      <%= report.mission.name %>
    </td>
  </tr>
  <tr>
    <td>
      <%= I18n.t('report/report.form') %>
    </td>
    <td>
      <%= report.form.name %>
    </td>
  </tr>
  <tr>
    <td>
      <%= I18n.t('report/report.response_count') %>
    </td>
    <td>
      <%= report.response_count %>
    </td>
  </tr>
  <tr>
    <td>
      <%= I18n.t('report/report.missing_observers') %>
    </td>
    <td>
      <%= report.observers_without_responses.map(function(obs){ return obs.name; }).join(', ') %>
    </td>
  </tr>
</table>

<table class="std_form_report">
  <% /*# for now we just have one group */ %>
  <% report.groups[0].clusters.forEach(function(cluster){ %>
    
    <% /*# do cluster header row */ %>
    <tr class="cluster_header">
      <td>#</td>

      <% /*# if full width display type, header cell must be extra wide */ %>
      <td <%= cluster.display_type == 'full_width' ? 'colspan="' + (max_cols + 1) + '"' : '' %>>
        <%= I18n.t('activerecord.models.question.one') %>
      </td>

      <% /*# do rest of header cells if necessary */ %>
      <% if (cluster.display_type == 'flow' || cluster.display_type == 'structured' && cluster.headers.length > max_cols) { %>
        <td colspan="<%= max_cols %>"><%= cluster.overall_header %></td>
      <% } else if (cluster.display_type == 'structured') { %>
        <% /*# print out each header cell */ %>
        <% cluster.headers.forEach(function(header) { %>
          <td><%= helper.partial('header', {header: header}) %></td>
        <% }); %>

        <% /*# print out blank cell to fill up to max */ %>
        <% if (max_cols > cluster.headers.length) { %><td colspan="<%= max_cols - cluster.headers.length %>"></td><% } %>
      <% } %>
    </tr>

    <% /*# do summary rows */ %>
    <% cluster.summaries.forEach(function(summary) { %>

      <% /*# if this is a full_width summary (and it has matching items), it gets multiple rows, one item per row */ %>
      <% if (cluster.display_type == 'full_width' && summary.items.length > 0) { %>

        <tr>
          <td><%= summary.questioning.rank %></td>
          <td colspan="<%= max_cols + 2 %>">
            <%= report.question_labels == 'title' ? summary.questioning.name : summary.questioning.code %>
          </td>
        </tr>

        <% summary.items.forEach(function(item) { %>
          <tr>
            <td colspan="<%= max_cols + 2 %>">
              <%= helper.partial('item', {item: item}) %>
            </td>
          </tr>
        <% }); %>

      <% } else { %>

        <tr>
          <td><%= summary.questioning.rank %></td>
          <td><%= report.question_labels == 'title' ? summary.questioning.name : summary.questioning.code %></td>

          <% /*# if flow type, or if any other type and items are empty, just print the responses sep'd by commas */ %>
          <% if (cluster.display_type == 'flow' || summary.items.length == 0) { %>

            <td colspan="<%= max_cols %>">
              <% if (summary.items.length == 0) { %><%= I18n.t('report/report.no_match') %><% } %>

              <%= summary.items.map(function(item) { return '"' + item.text + '"'; }).join(', ') %>
            </td>

          <% /*# if structured type with less than max_cols, do separate cells */ %>
          <% } else if (cluster.display_type == 'structured' && cluster.headers.length <= max_cols) { %>

            <% summary.items.forEach(function(item) { %>
              <td><%= helper.partial('item', {item: item}) %></td>
            <% }); %>

            <% /*# print out blank cell to fill up to max */ %>
            <% if (max_cols > cluster.headers.length) { %><td colspan="<%= max_cols - cluster.headers.length %>"></td><% } %>

          <% /*# if structured type with more than max_cols, do one big cell */ %>
          <% } else if (cluster.display_type == 'structured' && cluster.headers.length > max_cols) { %>

            <td colspan="<%= max_cols %>">
              <%= summary.items.map(function(item, idx) { return helper.partial('item_with_header', {item: item, header: cluster.headers[idx]}); }).join(', ') %>
            </td>

            <% /*# print out blank cell to fill up to max */ %>
            <% if (max_cols > cluster.headers.length) { %><td colspan="<%= max_cols - cluster.headers.length %>"></td><% } %>

          <% } %>
        </tr>
      <% } %>
    <% }); %>
  <% }); %>
</table>